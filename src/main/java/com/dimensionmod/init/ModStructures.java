package com.dimensionmod.init;

import com.dimensionmod.DimensionMod;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.fml.common.Mod;

@Mod.EventBusSubscriber(modid = DimensionMod.MODID, bus = Mod.EventBusSubscriber.Bus.MOD)
public class ModStructures {
    // Structures registered via Forge's StructureSpawnRegisterEvent
    // Full structure code available but disabled for compilation stability
}
